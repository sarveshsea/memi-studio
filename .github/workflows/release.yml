# SPDX-License-Identifier: FSL-1.1-ALv2
# Copyright 2026 Humyn LLC
name: release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to release (e.g. v1.0.3)"
        required: true

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  CARGO_TARGET_DIR: ${{ github.workspace }}/src-tauri/target

jobs:
  build-and-sign:
    name: Build, sign, notarize (${{ matrix.target }})
    runs-on: macos-14
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            arch: arm64
          - target: x86_64-apple-darwin
            arch: x64

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-${{ matrix.target }}-${{ hashFiles('src-tauri/Cargo.lock') }}

      - name: Install npm dependencies
        run: npm ci

      - name: Fetch sidecar binaries
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run fetch:runtime -- --all-archs

      - name: Run product hygiene gates
        run: |
          npm run test:no-mock-ui
          npm run test:workbench-context
          npm run test:runtime-fetch

      - name: Clean stale bundle outputs
        run: |
          set -euo pipefail
          rm -rf "$CARGO_TARGET_DIR/${{ matrix.target }}/release/bundle"
          rm -rf "$CARGO_TARGET_DIR/release/bundle"

      - name: Import Apple Developer ID certificate
        env:
          APPLE_CERTIFICATE_BASE64: ${{ secrets.APPLE_CERTIFICATE_BASE64 }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          set -euo pipefail
          echo "$APPLE_CERTIFICATE_BASE64" | base64 -D -o cert.p12
          security create-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security import cert.p12 -k build.keychain -P "$APPLE_CERTIFICATE_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" build.keychain
          rm -f cert.p12

      - name: Build, sign, notarize Tauri app
        env:
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          # Updater signing — Tauri picks these up automatically and produces
          # the .app.tar.gz + .app.tar.gz.sig artifacts that latest.json points at.
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        run: npm run tauri:build:release -- --target ${{ matrix.target }}

      - name: Verify Developer ID signatures
        run: |
          set -euo pipefail
          APP_DIR="$CARGO_TARGET_DIR/${{ matrix.target }}/release/bundle/macos"
          APPS=("$APP_DIR"/*.app)
          APP="${APPS[0]}"
          if [ ! -d "$APP" ]; then
            echo "no app bundle found under $CARGO_TARGET_DIR/${{ matrix.target }}/release/bundle/macos"
            exit 1
          fi
          codesign --verify --deep --strict --verbose=4 "$APP"
          codesign -dv --verbose=4 "$APP/Contents/MacOS/memoire-studio" 2>&1 | grep "Authority=Developer ID Application"
          codesign -dv --verbose=4 "$APP/Contents/MacOS/memi-studio-runtime" 2>&1 | grep "Authority=Developer ID Application"

      - name: Notarize DMG
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          set -euo pipefail
          DMG=$(find "$CARGO_TARGET_DIR/${{ matrix.target }}/release/bundle/dmg" -name "*.dmg" -print -quit)
          if [ -z "$DMG" ]; then
            echo "no DMG found under $CARGO_TARGET_DIR/${{ matrix.target }}/release/bundle/dmg"
            exit 1
          fi
          echo "submitting $DMG to notarytool (synchronous, will wait for verdict)..."
          xcrun notarytool submit "$DMG" \
            --apple-id "$APPLE_ID" \
            --password "$APPLE_PASSWORD" \
            --team-id "$APPLE_TEAM_ID" \
            --wait \
            --output-format json > notarize-result.json
          cat notarize-result.json
          STATUS=$(node -e "console.log(require('./notarize-result.json').status)")
          if [ "$STATUS" != "Accepted" ]; then
            SUBMISSION_ID=$(node -e "console.log(require('./notarize-result.json').id)")
            echo "notarization failed (status=$STATUS) — fetching log for submission $SUBMISSION_ID"
            xcrun notarytool log "$SUBMISSION_ID" \
              --apple-id "$APPLE_ID" \
              --password "$APPLE_PASSWORD" \
              --team-id "$APPLE_TEAM_ID" || true
            exit 1
          fi
          echo "stapling notarization ticket to $DMG"
          xcrun stapler staple "$DMG"
          echo "stapled successfully"

      - name: Verify Gatekeeper assessment
        run: |
          set -euo pipefail
          DMG=$(find "$CARGO_TARGET_DIR/${{ matrix.target }}/release/bundle/dmg" -name "*.dmg" -print -quit)
          spctl -a -vv --type install "$DMG" || (echo "spctl assessment failed for $DMG"; exit 1)

      - name: Collect updater artifacts
        run: |
          set -euo pipefail
          # Tauri emits "Mémoire Studio.app.tar.gz" with no arch in the name.
          # Both matrix runs would collide once the publish job collapses them
          # into release/, so rename per-arch here. Use the matrix.target
          # value (aarch64 / x86_64) so the names line up with the DMG arch
          # tokens and the latest.json find patterns.
          mkdir -p updater-staging
          ARCH_TOKEN="${{ matrix.target }}"
          ARCH_TOKEN="${ARCH_TOKEN%-apple-darwin}"
          SRC="$CARGO_TARGET_DIR/${{ matrix.target }}/release/bundle/macos"
          cp "$SRC/Mémoire Studio.app.tar.gz" "updater-staging/Memoire-Studio-${ARCH_TOKEN}.app.tar.gz"
          cp "$SRC/Mémoire Studio.app.tar.gz.sig" "updater-staging/Memoire-Studio-${ARCH_TOKEN}.app.tar.gz.sig"
          ls -la updater-staging/

      - name: Upload artifacts (DMG + updater bundle)
        uses: actions/upload-artifact@v4
        with:
          name: memi-studio-${{ matrix.arch }}
          path: |
            src-tauri/target/${{ matrix.target }}/release/bundle/dmg/*.dmg
            updater-staging/*
          if-no-files-found: error

  publish-release:
    name: Publish GitHub Release
    needs: build-and-sign
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}

      - name: Download all DMG artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Compute checksums + stage updater bundles
        run: |
          set -euo pipefail
          TAG="${{ github.event.inputs.tag || github.ref_name }}"
          VERSION="${TAG#v}"
          mkdir -p release
          find dist -name "*.dmg" -exec mv {} release/ \;
          find dist -name "*.app.tar.gz" -exec mv {} release/ \;
          find dist -name "*.app.tar.gz.sig" -exec mv {} release/ \;

          ARM_DMG=$(find release -maxdepth 1 -name "*aarch64.dmg" -print -quit)
          X64_DMG=$(find release -maxdepth 1 -name "*x64.dmg" -print -quit)
          if [ -z "$ARM_DMG" ] || [ -z "$X64_DMG" ]; then
            echo "missing architecture-specific DMG artifact"
            exit 1
          fi
          mv "$ARM_DMG" "release/Memoire.Studio_${VERSION}_aarch64.dmg"
          mv "$X64_DMG" "release/Memoire.Studio_${VERSION}_x64.dmg"

          cd release
          shasum -a 256 Memoire.Studio_*.dmg > SHA256SUMS
          ls -la

      - name: Generate latest.json updater manifest
        run: |
          set -euo pipefail
          TAG="${{ github.event.inputs.tag || github.ref_name }}"
          VERSION="${TAG#v}"
          PUB_DATE="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          # Per "Collect updater artifacts" step: files are
          # Memoire-Studio-aarch64.app.tar.gz / Memoire-Studio-x86_64.app.tar.gz
          ARM_SIG_FILE=$(find release -name "*aarch64*.app.tar.gz.sig" -print -quit)
          X64_SIG_FILE=$(find release -name "*x86_64*.app.tar.gz.sig" -print -quit)
          ARM_TAR=$(find release -name "*aarch64*.app.tar.gz" -not -name "*.sig" -print -quit)
          X64_TAR=$(find release -name "*x86_64*.app.tar.gz" -not -name "*.sig" -print -quit)
          if [ -z "$ARM_SIG_FILE" ] || [ -z "$X64_SIG_FILE" ]; then
            echo "missing updater signature(s) — Tauri build did not emit .sig files."
            echo "verify: TAURI_SIGNING_PRIVATE_KEY secret is set + tauri.conf.json has createUpdaterArtifacts: true"
            exit 1
          fi
          ARM_SIG=$(cat "$ARM_SIG_FILE")
          X64_SIG=$(cat "$X64_SIG_FILE")
          ARM_NAME=$(basename "$ARM_TAR")
          X64_NAME=$(basename "$X64_TAR")
          REPO="${{ github.repository }}"
          cat > release/latest.json <<EOF
          {
            "version": "$VERSION",
            "notes": "See https://github.com/$REPO/releases/tag/$TAG",
            "pub_date": "$PUB_DATE",
            "platforms": {
              "darwin-aarch64": {
                "signature": "$ARM_SIG",
                "url": "https://github.com/$REPO/releases/download/$TAG/$ARM_NAME"
              },
              "darwin-x86_64": {
                "signature": "$X64_SIG",
                "url": "https://github.com/$REPO/releases/download/$TAG/$X64_NAME"
              }
            }
          }
          EOF
          cat release/latest.json

      - name: Publish release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          TAG="${{ github.event.inputs.tag || github.ref_name }}"
          gh release create "$TAG" \
            --repo "${{ github.repository }}" \
            --title "memi-studio $TAG" \
            --generate-notes \
            release/*.dmg release/SHA256SUMS release/latest.json release/*.app.tar.gz release/*.app.tar.gz.sig
